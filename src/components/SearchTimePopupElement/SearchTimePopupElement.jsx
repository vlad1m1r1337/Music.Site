import {MenuItem} from './SearchTimePopupElement.styles'

export default function SearchTimePopupElement({item}) {
    return (
        <div>
            <MenuItem >{item}</MenuItem>
        </div>
    )
}